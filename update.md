# 更新项

本文档记录当前必须优先修复的问题，以及每一轮任务落地后的更新时间和内容。

## 对话更新记录

### 2026-04-30 11:05:00 CST

- 新增体验改造主线到 `limited.md`
- 已登记 5 项后续任务：
  - 聊天页重构和整体美化
  - 绑定改弹窗
  - 顶部快捷按钮改成“做饭吗？/想你！”
  - 登录页美化
  - 送达 / 已读后端与前端联调
- 已完成第 1 项聊天页重构和整体美化
- 前端聊天页已重做顶部信息区、会话概览卡、消息区卡片、消息气泡层次和底部输入区视觉
- 本轮未开始绑定弹窗、快捷按钮文案替换、登录页美化和已读回执联调

### 2026-04-30 10:30:00 CST

- 完成局域网 / 多环境地址配置增强
- 前端新增 `runtime-config.js` 注入入口，支持用 `CLIENT_API_BASE_URL` 在启动时覆盖后端地址
- 前端在未注入配置时，会按当前页面主机名自动推导 API 地址到 `:3000`
- 后端 CORS 已从固定 `127.0.0.1:5173` 改为可通过 `CORS_ALLOW_ORIGIN` 配置
- `README.md`、`limited.md` 已同步更新局域网启动与验证方式

### 2026-04-30 10:14:19 CST

- 新增交接文档
- 已补充当前项目状态、关键实现、已知限制、推荐下一步优先级和验证方式
- 交接文档路径：
  - `handoff.md`

### 2026-04-30 10:05:15 CST

- 完成历史消息分页加载更多
- `GET /messages/conversation` 已支持 `cursor`、`nextCursor` 和 `hasMore`
- 前端消息区已支持“加载更多消息”按钮，能继续拉取更早历史
- 完成页内通知
- 新消息到达时会显示页内通知条，页面隐藏时会同步更新标题未读计数
- 完成 30 天定时清理
- 服务端启动后会先执行一次老消息清理，并按固定间隔继续清理
- `limited.md` 与 `README.md` 已同步更新当前能力和剩余限制

### 2026-04-30 09:50:00 CST

- 完成表情发送增强
- 表情面板从固定 6 个按钮升级为分类面板
- 新增最近使用表情，会按当前登录用户写入本地存储
- 新增笑脸、手势、喜欢、气氛四组常用表情分类
- `limited.md` 已同步更新表情能力剩余限制

### 2026-04-30 09:39:40 CST

- 完成图片发送增强
- 前端图片发送已支持自动缩放与压缩，不再要求用户先把原图压到很小
- 原图大小上限调整为 `8 MB`
- 发送目标体积调整为约 `220 KB`
- 压缩后仍过大的图片会在前端直接提示失败原因
- `README.md` 与 `limited.md` 已同步更新当前能力和剩余限制

### 后续记录规则

- 从本次开始，每一次对话里落地的任务，都追加记录到本文件
- 记录至少包含：更新时间、完成内容、涉及范围

## 今天必须修

- [x] 轮询只允许把当前会话的新消息追加到当前聊天页，避免串入其他会话消息
- [x] 修复 `poll` 使用 `serverTime` 时的时间窗口漏消息问题
- [x] 发送消息前校验发送方是否已建立绑定
- [x] 为关键输入和请求体补基础长度上限

## 本轮已完成

### 1. 轮询只收当前会话消息

- 前端轮询结果追加前，已按当前 `targetUserId` 对应的 `conversationId` 过滤
- 不属于当前聊天页的消息，不再混入当前消息列表

### 2. 修复轮询漏消息窗口

- 后端 `poll` 现在会在查询开始时记录一个固定的 `serverTime`
- 查询只返回 `sent_at > since` 且 `sent_at <= serverTime` 的消息
- 下一次轮询继续使用这个固定 `serverTime`，避免“查询后到返回前”落库的消息被跳过

### 3. 发送前绑定校验

- 已要求发送方先建立 `senderId -> receiverId` 绑定
- 未绑定时，`POST /messages/text` 会直接拒绝发送

### 4. 输入与请求体长度上限

- `userId` 相关字段最大长度为 64
- `text` 最大长度为 1000
- `GET /messages/conversation` 的 `limit` 最大为 100
- JSON 请求体最大为 16 KB
